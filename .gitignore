# Ignore everything
**

# Un-ignore only root-level files (no folders or their contents)
!/*

# Re-ignore all files inside subdirectories
**/*

# Specifically ignore sensitive configuration file (exact match only)
/gpp.ini

# Explicitly allow the template file
!/gpp.template.ini